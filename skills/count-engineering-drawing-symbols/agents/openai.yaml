interface:
  display_name: "AI Engineering Drawing Estimator"
  short_description: "Count and review DWG/PDF engineering symbols"
  default_prompt: "Use $count-engineering-drawing-symbols to count socket and data symbols in this engineering drawing with auditable markup."
dependencies:
  tools:
    - type: "mcp"
      value: "engineering-drawing-estimator"
      description: "Local PDF vector extraction, candidate detection, review, and markup tools"
      transport: "stdio"
