interface:
  display_name: "AI Engineering Drawing Estimator"
  short_description: "Review Power and Fire Alarm drawing quantities"
  default_prompt: "Use $count-engineering-drawing-symbols to prepare and review a Power or Fire Alarm symbol audit with markup. Ask me about every uncertain point before finalizing."
dependencies:
  tools:
    - type: "mcp"
      value: "engineering-drawing-estimator"
      description: "Local PDF vector extraction, candidate detection, review, and markup tools"
      transport: "stdio"
